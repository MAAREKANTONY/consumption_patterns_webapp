# === BigQuery (optional: only needed if you use /sales/query/* or /score/* endpoints) ===
# GCP project that hosts the dataset
GCP_PROJECT=

# BigQuery dataset name (default in our SQL examples)
BQ_DATASET=datafyre_my_lemonade

# Optional: override if you prefer using GOOGLE_CLOUD_PROJECT naming
# GOOGLE_CLOUD_PROJECT=
